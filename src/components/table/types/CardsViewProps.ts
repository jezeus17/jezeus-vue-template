
export default interface CardsViewProps {
  paginationOptions?: Array<number>,
  gridClass?: string,
  gridOptions?: {
    base: number,
    sm: number,
    md: number,
    lg: number,
    xl: number,
    xxl: number
  },
}
